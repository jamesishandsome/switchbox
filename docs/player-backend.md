# PlayerBackend

SwitchBox ?????????? `PlayerBackend`?

```text
TVBox episode/live URL -> PlaybackRequest -> PlayerBackend::play -> PlaybackState
```

## ?????Switch WebApplet

Switch ????`source/PlayerBackend.cpp` ??? libnx WebApplet?

- `webPageCreate(url)`
- `webConfigSetBootAsMediaPlayer(true)`
- `webConfigSetMediaAutoPlay(true)`
- `webConfigSetMediaPlayerSpeedControl(true)`
- `webConfigShow(...)`

??????? WebApplet ???????????? MP4/HLS URL????????????? WebApplet?

## ??

WebApplet ??????????

- ????? M3U8/HLS ?????
- ??? mpv ???????????????
- ??? DRM/Widevine?
- ???? full-memory homebrew ???????? applet mode?

## ???????

?????? mpv/FFmpeg/nxmp ????????????

```cpp
bool PlayerBackend::play(const PlaybackRequest& request,
                         PlaybackState& state,
                         std::string& error) const;
```

???

- `request.title`: UI ?????
- `request.url`: ???HLS URL ???????????
- `state.accepted`: ?????????
- `state.rendersVideo`: ???????????
- `state.message`: ? UI ????????


## Companion Server ??

? `settings.json` ????

```json
{
  "companionBaseUrl": "http://192.168.1.10:8099",
  "playbackMode": "auto"
}
```

Switch ?????????????

```text
http://192.168.1.10:8099/play?mode=auto&url=<encoded-original-url>
```

?????

- `direct`: ????
- `proxy`: ? companion HTTP ?????
- `transcode`: ? companion FFmpeg ? HLS?
- `auto`: ???????????????? Switch ? ZR ?? `http://server:8099|transcode`?

## HLS playlist ??

Companion `/proxy` ??? `.m3u8` / `mpegurl` ???????????? URL ? `#EXT-X-KEY URI` ??? `/proxy?url=...`?????????? Switch ???????????

## TVBox header ??

Companion Server ???? TVBox ???????

```text
https://example/video.m3u8|User-Agent=xxx&Referer=https%3A%2F%2Fexample%2F
```

`/proxy` ??????????????HLS playlist ??????? header ??????????????? URI???? header ??? `User-Agent`?`Referer`/`Referrer`?`Origin`?`Cookie`?`Authorization`?
